# AI Slot Machine (vanilla web)

Open `index.html` in a browser to play. Uses only HTML, CSS, JS and localStorage for tokens.

- Pull `Pull Lever (Spin)` to play.
- `Cheer on AI (+10 tokens)` adds tokens (to mock the AI economy).

Have fun mocking the model and managing tokens.
